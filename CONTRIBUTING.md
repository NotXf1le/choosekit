# Contributing

Use Node.js 20 or newer.

## Core package

```sh
npm ci
npm run check
npm pack --dry-run
```

## MCP package

```sh
cd packages/choosekit-mcp
npm ci
npm run check
npm pack --dry-run
```
